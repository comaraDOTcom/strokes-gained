node_modules
.next
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
.env
.env.local
*.tsbuildinfo
next-env.d.ts
data/pglite

# Benchmark source screenshots (other golfers' round histories). NEVER commit these — only the
# anonymised counts in src/lib/insights/benchmark-data/ go in the repo.
benchmarks-raw/
.vercel

# Scratch and intermediate files (see CLAUDE.md)
archive/
# Subagent worktrees created by the Agent tool
.claude/worktrees/
